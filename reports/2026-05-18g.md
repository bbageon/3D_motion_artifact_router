# 연구일지 — 2026-05-18 (새벽, Task 3 — Refinement Loop Integration Smoke)

> 본 일지는 2026-05-18 의 일곱 번째 entry. 사용자 권장 순서의 마지막 Task 3 — `refinement_loop/loop.py` 구현 + integration smoke 1 sample (3 artifact). AGENTS.md §6-13 의무: 본 결과는 가설 supports/contradicts 의 근거 아닌 integration test 만.

## 요약

`refinement_loop/loop.py` 의 stub 을 완전 구현 (Score 비감소 의무 + rollback + KDG ordering 사전 hook 자리 + decision/score trace 박제). 7 unit tests (누적 58/58) + 3 종 artifact 의 single-sample integration smoke 모두 다른 종료 분기 (rollback / converged-clean / max_iter) 정상 검증. AGENTS.md §6-13 의무 — raw record 의 `record_type="loop_integration_smoke"` 가 가설 평가 단계에서 제외 표식.

## 주요 진행사항

- ✅ **`refinement_loop/loop.py` 구현** — 사용자 권장 Task 3. `RefinementLoop.run` 의 의사코드 (명세 §6.5) 가 실제 작동. AGENTS.md §3-4 Score 비감소 의무 (Score increase tolerance 검사 + rollback) 의 직접 구현. 이유: closed-loop refinement 가 [H-2026-204](../evals/hypotheses/H-2026-204.md) (RQ1+RQ2 — artifact-conditioned tool selection + closed-loop 가 fixed post-processing 보다 NetGain 우위) 의 main RQ2 의 핵심 메커니즘.
- ✅ **7 unit tests + 58/58 누적 PASS** — clean motion 즉시 STOP / corrupted 감소 / max_iter 종료 / unknown tool break / Score 악화 rollback / 잘못된 shape rejection / score_trace 와 tool_history 길이 일관성.
- ✅ **`tools/loop_integration_smoke.py` CLI 구현** — AGENTS.md §6-13 의무를 코드 docstring + raw record 의 `record_type="loop_integration_smoke"` + `agents_md_obligation_note` field 로 박제. 가설 평가 시 본 type 의 record 가 자동 필터 가능.
- ✅ **3 종 artifact 의 1-sample integration smoke** — 3 가지 다른 종료 분기 정상 검증. raw record 6 개 (각 artifact 별 1-2 records).

## 파악한 부분 — Integration smoke 결과 (1 sample, 000000)

| Artifact | 종료 분기 | n_tool_applications | Score trace |
|---|---|---|---|
| `foot_floating` | **rollback** (Score 비감소 의무) | 0 (rolled back before accepting) | [0.523] |
| `bone_stretch_right_arm` | **converged-clean** (artifacts 0 도달) | 3 | [0.191, 0.060, 0.009, 0.000] |
| `global_jitter` | **max_iter 도달** | 5 | [1.955, 1.765, 1.563, 1.400, 0.981, 0.806] |

세 결과의 의미:

1. **`foot_floating` rollback** — FootLockTool 적용 후 본 sample 의 total score 가 증가 (cross-evaluator delta 가 foot_lock 의 target 감소보다 큰 폭으로 다른 evaluator 를 악화). AGENTS.md §3-4 의무 작동 — rollback 후 loop 종료, 직전 motion (corrupted 그대로) 반환. **Guard 5 (AGENTS.md §6-12) 의 cross-evaluator side effects 의 실제 영향 확인** — single objective 만 최적화하면 다른 evaluator 가 악화되는 trade-off 가 발생함을 실증적으로 박제.
2. **`bone_stretch_right_arm` converged-clean** — BoneProjectionTool 을 3 step 적용해 partial-frame bone variation 을 완전히 제거 (Score 0). `stop_reason="no_evaluator_reports"` (evaluator 가 더 이상 보고 안 함). closed-loop 가 의도된 대로 작동.
3. **`global_jitter` max_iter** — VelocitySmoothingTool 을 5 step 적용해 score 가 1.955 → 0.806 으로 감소했지만 max_iterations 도달 전에 STOP 도달 못 함. max_iterations 를 더 크게 잡거나 STOP 임계를 조정해야 함. 본 결과는 integration smoke 라 hypothesis 의 baseline 데이터로 사용 안 함.

### AGENTS.md §6-13 의무 박제 방식

각 raw record (`evals/raw/<timestamp>_loop_integration_smoke_v1_<id>.json`) 에 다음 필드:

- `record_type: "loop_integration_smoke"` — 가설 평가 단계 (eval-compare SKILL) 에서 본 type 자동 제외.
- `agents_md_obligation_note`: "AGENTS.md §6-13 — 본 record 의 record_type='loop_integration_smoke' 는 가설 supports/contradicts 의 근거로 인용 금지. integration smoke 통과 사실은 파이프라인이 죽지 않고 도는지의 확인일 뿐, 통계적 근거 아님."
- `metrics_not_applicable.NetGain`: "integration smoke; NetGain is not the test target".
- `metrics_not_applicable.netgain_weight_status`: "n/a (integration smoke)".

eval-compare 의 향후 5단계 리포트 (trial ≥ 20) 에서 `record_type != "loop_integration_smoke"` 필터링 권장 — 본 type 의 record 는 "integration test 결과" 절에 별도 인용.

## 정량 지표 (Generator · Tool Registry 별 분리)

| 측정 단위 | 값 |
|---|---|
| Generator | `humanml3d_gt` + synthetic injection (sample id 000000) |
| Tool registry | 3 종 (FootLockTool, BoneProjectionTool, VelocitySmoothingTool) |
| Evaluator registry | 3 종 (FootFloating v1.2.0, BoneLength v1.0.0, VelocityJitter v1.1.0) |
| Orchestrator | RuleBasedOrchestrator (stop_threshold='low') |
| max_iterations | 5 |
| score_increase_tolerance | 0.01 |
| n integration smokes | 3 (foot_floating, bone_stretch_right_arm, global_jitter) |
| Score 비감소 (rollback) 검증 | 1 case (foot_floating) |
| Converged-clean (artifacts → 0) | 1 case (bone_stretch_right_arm) |
| Max_iter 도달 | 1 case (global_jitter) |

NetGain 자체는 본 smoke 에서 미계산 — AGENTS.md §6-13 의무에 따라 가설 평가의 근거가 아님을 분명히 함.

## 시각화

(없음 — 본 일지는 integration smoke. tool call trace 시각화 (timeline bar chart) 는 본격 평가 단계에서 도입.)

## 실험 메타

- **사용 환경**: motion3d conda env (Python 3.10), pytest 9.0.3.
- **명령어**:
  ```
  python -m pytest tests/unit/ -v   # 58 passed
  python -m tools.loop_integration_smoke --sample-id 000000 \
      --artifact foot_floating --seed 42 --max-iterations 5
  python -m tools.loop_integration_smoke --sample-id 000000 \
      --artifact bone_stretch_right_arm --seed 42 --max-iterations 5
  python -m tools.loop_integration_smoke --sample-id 000000 \
      --artifact global_jitter --seed 42 --max-iterations 5
  ```
- **시드**: 42.
- **신규 파일**:
  - `refinement_loop/loop.py` (stub 채움).
  - `tools/loop_integration_smoke.py` (CLI).
  - `tests/unit/refinement_loop/__init__.py`, `tests/unit/refinement_loop/test_loop.py` (7 tests).
  - `evals/raw/<timestamp>_loop_integration_smoke_v1_000000.json` × 6 (3 artifact × 2 run: 첫 3 개는 unicode 이슈 fix 전 successful file write + 실패 stdout, 후 3 개는 fix 후).
  - `reports/2026-05-18g.md` (본 일지).

## raw record

- 6 raw record. 모두 `record_type="loop_integration_smoke"` + `agents_md_obligation_note` 박제.
- AGENTS.md §3-15 raw record metadata 의무: severity_versions + split_id + evaluator_config_hashes + tool_registry_config_hashes 모두 충족.
- AGENTS.md §6-13 가설 근거 인용 금지: record 자체에 명시.

활성 가설 (참고):
- [H-2026-203](../evals/hypotheses/H-2026-203.md) (no-harm — high-quality input 에 abstain/STOP) — 본 smoke 는 corrupted 입력의 test 이라 no-harm 검증과 직접 무관.
- [H-2026-204](../evals/hypotheses/H-2026-204.md) (RQ1+RQ2 — closed-loop 가 fixed post-processing 보다 NetGain 우위) — 본 smoke 는 closed-loop 파이프라인이 작동함의 확인 (integration test). 가설 근거 인용 금지.
- [H-2026-205](../evals/hypotheses/H-2026-205.md) (RQ3 — learnable routing), [H-2026-206](../evals/hypotheses/H-2026-206.md) (RQ4 — generator-agnostic 일반화) — 본 smoke 는 rule_based orchestrator 만 사용, 추후 learned selector + transfer 측정 단계에서 cross-link.

## 우회 발견 (workaround-tracking ledger)

오늘 등록·전환된 항목:

- **(noted-issue, ledger 미등록)** — Windows cp949 stdout 환경에서 unicode em-dash (`—`) 출력 시 `UnicodeEncodeError`. tools/loop_integration_smoke.py 의 print 문에서 발생. 해결: print 문의 unicode dash 를 ASCII 하이픈으로 교체 (raw record JSON 파일은 utf-8 명시라 영향 없음). severity: low (도구의 stdout 만 영향, raw record 의 정확성은 영향 없음). ledger 등록 대상 아님 (cosmetic 문제).

## 실패한 시도 (Negative Results)

- **첫 3 회 smoke 실행 (unicode fix 전)** — file write 는 성공, 마지막 stdout 의 unicode em-dash 가 cp949 인코딩 실패로 traceback. 그 file 들이 valid raw record 라 6 개 모두 commit. fix 후 3 회 정상 실행.
  - 정량 근거: 6 raw record (3 unique timestamps × 2 runs)
  - 폐기 사유: cosmetic stdout 문제 (record 정확성 무영향).
  - 교훈: Windows 환경의 stdout 인코딩 (cp949 default) 고려. 본 프로젝트의 CLI 들은 print 문에 unicode 사용 시 ASCII fallback 또는 utf-8 명시 stdout 필요.

- **foot_floating 의 rollback (negative-result-flag 아님, design 동작)** — FootLockTool 1 회 적용으로 total score 가 증가 → AGENTS.md §3-4 Score 비감소 의무에 따라 rollback. 본 결과는 "tool 적용 실패" 가 아닌 "rule_based + single objective 의 한계 + Guard 5 cross-evaluator side effects 의 실제 영향" 의 실증. 사용자 directive 의 Guard 5 의무가 데이터로 검증됨.

## Tool Call Trace 요약 (bone_stretch_right_arm smoke 의 사례)

```
Step  | Tool                | Target Part   | Strength | Score after
------+---------------------+---------------+----------+------------
init  | -                   | -             | -        | 0.191
1     | BoneProjectionTool  | right_arm     | large    | 0.060
2     | BoneProjectionTool  | right_arm     | large    | 0.009
3     | BoneProjectionTool  | right_arm     | large    | 0.000
STOP  | -                   | -             | -        | -
```

`global_jitter` 의 trace 도 5 step VelocitySmoothingTool 의 연속 적용. trace 시각화는 본격 평가 단계에서.

## 다음 단계

1. **본 commit + push** (Task 3 — refinement_loop + integration smoke).
2. **Week 4 deliverable 의 종합 정리** — Task 1/2/3 의 결과를 누적해 보면 명세 §11 Go/Stop 기준 ([04-evaluation.md §10](../.claude/rules/phase/04-evaluation.md)) 의 일부가 baseline 으로 확보됨:
   - Go 기준 #4 ("artifact 별 effective tool 이 다르게 나타남, 최소 2 artifact type"): tool_effect_matrix v1 의 대각선 단조 감소 + off-diagonal 거의 0 으로 충족 (foot_floating ↔ FootLockTool, global_jitter ↔ VelocitySmoothingTool 짝짐).
   - Stop 기준 #5 ("모든 artifact 에 단일 best tool 우세 (conditional selection 가치 부재)"): oracle_single_step v1 의 best_tool 분포가 artifact 마다 다른 tool 을 best 로 선택 → 본 stop 기준 비해당.
3. **(follow-up)** sequence oracle (closed-loop oracle) — AGENTS.md §3-16 의 두 번째 type. closed-loop 가 본 single-step oracle 보다 더 나아질 수 있는지의 fair upper bound 측정.
4. **(follow-up)** rule_based 가 foot_floating 에서 rollback 발생한 원인 분석 — 본격 평가 전 cross-evaluator side effects 의 weight tuning 필요.
