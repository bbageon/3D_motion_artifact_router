# 2026-05-29 - Harness Guard + G-3 Synthetic Severe + RL-2 Stage 1 Q-surface + Stage A Transition Dataset

> Date: 2026-05-29.
> Scope: (1) §1-6 repo-local harness hardening. (2) §7 Step G-3 synthetic severe FID/R-Prec. (3) §8 RL-2 Stage 1 Q_safe(s, tool, u) grid surface + reranking. (4) §9 RL-2 Stage A action-effect transition dataset (dense u-grid, 4 distribution).

## 1. Summary

Step E/F snapshot writers now stamp common comparison metadata:

- `split_id`
- `oracle_type`
- `action_space_grid`
- `evidence_tier`
- `evaluator_config_hashes`
- `evaluator_severity_versions`

The B2 selection-name drift is also fixed in future Step F outputs:

- `B2-netgain-best`: max NetGain over B2-small/medium/large.
- `B2-artifact-best`: max artifact reduction over B2-small/medium/large.

## 2. Quantitative Metrics

No new inference/evaluation run was performed. Existing Step F numbers remain unchanged.

Validation commands:

```text
python -m py_compile tools/harness_metadata.py tools/safe_sequence_oracle_run.py tools/safe_sequence_oracle_synthetic_run.py tools/rl2_build_training_data.py tools/rl2_train_imitation.py tools/rl2_closed_loop_eval.py tools/rl2_distribution_behavior.py tools/rl2_violation_decomposition.py tools/build_humanml3d_prompt_file.py
pytest tests/unit -q
```

## 3. Visualization

No new visualization was generated.

## 4. Experiment Meta

Changed files:

- `tools/harness_metadata.py`
- `tools/safe_sequence_oracle_run.py`
- `tools/safe_sequence_oracle_synthetic_run.py`
- `tools/rl2_build_training_data.py`
- `tools/rl2_train_imitation.py`
- `tools/rl2_closed_loop_eval.py`
- `tools/rl2_distribution_behavior.py`
- `tools/rl2_violation_decomposition.py`
- `.gitignore`

Existing large G2 generated outputs are ignored via `external_assets/g2_generated*/`.

## 5. Raw Record Cross-Link

No raw records were created or modified. This change only affects future Step E/F snapshot writers.

## 6. Intent-Reconciliation

**Intent-Reconciliation** (AGENTS.md §3-23):

- **Intent**: User directive (2026-05-29): stamp Step E/F snapshots with common metadata and disambiguate B2 naming before the next evaluation step.
- **Acceptance Criteria**: future Step E/F writers include split/action/oracle/evidence/evaluator metadata; B2 aggregate names distinguish NetGain selection from artifact selection; no historical numeric result is rewritten.
- **Actual**: common metadata helper added and wired into Step E/F snapshot tools; `rl2_closed_loop_eval` now emits `b2_netgain_best`; `rl2_violation_decomposition` now emits `B2-artifact-best`; historical snapshots were not reserialized.
- **Verdict**: `aligned`.
- **Side-effect**: None observed. This is repo-local bookkeeping and does not change experimental conclusions.

---

## 7. Step G-3 — Synthetic Severe Standard Metric (FID/Diversity + R-Precision/MM-Dist)

### 7-1. 사용자 directive 박제

> "G-3 synthetic severe는 그 다음. b2_netgain_best degradation 이 여기서 보일 것
>  (NetGain-best 가 over-smooth 하는 영역)."

G-1/G-2 (G2 natural, low/moderate severity) 와 **정반대 regime** 측정. synthetic severe =
clean HumanML3D 에 `foot_floating(0.08) + jitter(0.05)` 주입 (high severity) — corruption 이
분포 를 크게 밀어내므로 correction 이 실제로 **필요한** 영역.

### 7-2. 정량 지표 (n=60 per corrected method, GT_ref n=300)

표준 metric = HumanML3D tm2t evaluator (Guo 2022 CVPR, **Category A**). FID lower / R-Precision higher / MM-Dist lower = better.

| method | FID↓ vs GT | Diversity | R@1↑ | R@2↑ | R@3↑ | MM-Dist↓ |
|---|---|---|---|---|---|---|
| GT_ref | (ref) | 8.84 | — | — | — | — |
| **noop (corrupted)** | **57.25** | 2.02 | 0.103 | 0.200 | 0.270 | 7.72 |
| b2_netgain_best | 24.28 | 4.48 | 0.242 | 0.367 | 0.470 | 5.74 |
| b2_artifact_best | 30.68 | 3.80 | 0.202 | 0.298 | 0.370 | 6.37 |
| safe_oracle_3level | 13.47 | 6.29 | 0.253 | 0.381 | 0.456 | 5.22 |
| **safe_oracle_5level** | **9.86** | 6.76 | **0.314** | **0.433** | **0.522** | **4.62** |

raw record: [evals/snapshots/standard_metric_synthetic_severe_v1.json](../evals/snapshots/standard_metric_synthetic_severe_v1.json).
재현: `conda activate mgpt; python tools/standard_metric_synthetic_severe.py` (seed=42 injection, gt_seed=43).

### 7-3. 파악한 부분 (해석)

1. **noop(corrupted) 가 catastrophic** (FID 57.25, R@1 0.103, Diversity 2.02 collapse). high-severity 영역에서는 correction 이 **필수** — G2 natural 에서 noop 이 이미 좋았던 것과 정반대.
2. **safe oracle 이 압도적 복원**: 5-level FID 9.86 (noop 의 1/6, GT_ref 인접), R@1 0.314. 3-level FID 13.47. **single-tool full-body smoothing (B2) 을 크게 능가** — 이 regime 의 핵심은 **multi-step targeted gate-aware correction**.
3. **5-level oracle > 3-level oracle** (FID 9.86 < 13.47). high severity 는 finer/aggressive correction 이 유리 — "5-level = oracle ceiling" framing (AGENTS.md §3-21) 과 일관.

### 7-4. 사용자 예측 과의 divergence (정직 보고, AGENTS.md §3-13)

사용자 예측: "b2_netgain_best degradation 이 여기서 보일 것 (over-smooth)".

**실제**: synthetic severe 에서 b2_netgain_best 는 over-smooth 로 degrade 하지 **않았다**.
오히려 b2_artifact_best 보다 **우월** (FID 24.28 < 30.68, R@1 0.242 > 0.202). 이유: synthetic
severe 의 NetGain = **Protocol A** (clean GT reference, `α=5.0` MPJPE-to-clean penalty) — NetGain
selection 이 fidelity-aware 라 over-smoothing 을 억제. 반면 artifact-best 는 fidelity 무관
artifact-min 이라 over-smooth.

즉 b2_netgain_best 의 over-smooth degradation 은 **G2 natural (Protocol B, fidelity penalty
약함) 영역** 에서 나타나는 것이 아니라, **Protocol A 가 없는 (= clean reference 없는)** 영역
에서 발생할 현상. synthetic 은 clean GT 가 있어 Protocol A 가 가능하므로 degradation 이 억제됨.
**정정된 finding**: high severity 영역의 핵심 대비 는 "B2-netgain vs B2-artifact" 가 아니라
**"single-tool B2 family (24~31 FID) vs gate-aware multi-step safe oracle (10~13 FID)"**.

### 7-5. 종합 — safe oracle 의 regime-invariant 우월 (3-axis evidence)

| regime | noop | B2 family | safe oracle |
|---|---|---|---|
| G2 natural (low severity, §G-1/G-2) | 이미 좋음 (FID 0.575) | **degrade** (artifact-best 0.712) | ≈ noop **보존** (0.575) |
| synthetic severe (high severity, §G-3) | **catastrophic** (FID 57) | 부분 복원 (24~31) | **압도적 복원** (10~13) |

→ **safe oracle 만 두 regime 모두에서 우월**: 저severity 에서 보존, 고severity 에서 복원.
B2 family 는 저severity 에서 해치고 고severity 에서 불충분.

### 7-6. 한계 (caveat)

- **controlled diagnostic only** (synthetic, AGENTS.md §3-17). 최종 성능 sole evidence 금지. primary evidence = G-1/G-2 (G2 **real-distribution**).
- n=60 per corrected method → FID 추정 noisy. 단 magnitude (57 → 10) 가 noise 를 크게 상회.
- safe oracle = **oracle ceiling** (learned policy 아님). learned policy headroom 은 별도 (AGENTS.md §3-21).

### 7-7. Intent-Reconciliation (AGENTS.md §3-23)

- **Intent**: 사용자 directive — synthetic severe 영역의 standard metric 측정, b2_netgain_best over-smooth degradation 확인.
- **Acceptance criteria**: synthetic severe corpus 의 FID/R-Prec/MM-Dist (noop/B2 dual/safe oracle 3·5level), Category A 표준 evaluator.
- **Actual outcome**: 6 method × (FID/Diversity/R-Prec/MM-Dist) 계산 완료. safe oracle regime-invariant 우월 confirm. **단 b2_netgain_best degradation 예측 은 divergence** — Protocol A fidelity-awareness 로 over-smooth 억제됨 (§7-4 정직 보고).
- **Verdict**: **`aligned`** (metric pipeline + regime contrast 정확 산출). 단 사용자 예측 과의 divergence 1건 보고 의무 충족 (§7-4).

---

## 8. RL-2 Stage 1 — Bounded Continuous Action-Effect Surface `Q_safe(s, tool, u)`

### 8-1. 사용자 directive 박제 (연구 방향 전환)

> "policy(s) → action class 가 아니라 `Q_safe(s, tool, u)` 를 학습한다. u ∈ [0,1] 은 bounded
>  continuous intervention intensity. continuous 를 목표로 하되, 데이터는 grid-sampled action
>  effects 로 만든다. 추천 다음 작업: Stage 1 — u_grid = {0.3, 0.6, 1.0}."

discrete action **classification** (imitation) → bounded continuous **action-effect surface** 학습 으로 RL-2 main formulation 전환. governance 등록: [docs/action_space_provenance.md §5-2](../docs/action_space_provenance.md), safe_utility = Category C ([docs/metric_provenance.md §4-1-1](../docs/metric_provenance.md)).

### 8-2. Stage 1 산출물

| 산출물 | 경로 |
|---|---|
| action-effect dataset | [evals/snapshots/rl2_q_surface_dataset_stage1_v1.json](../evals/snapshots/rl2_q_surface_dataset_stage1_v1.json) (603 state × 9 후보) |
| two-head Q + reranking eval | [evals/snapshots/rl2_q_surface_eval_stage1_v1.json](../evals/snapshots/rl2_q_surface_eval_stage1_v1.json) (+ eps sweep) |
| surface figure | [reports/figures/2026-05-29/q_surface_stage1.png](figures/2026-05-29/q_surface_stage1.png) |

도구: [tools/rl2_q_surface_build.py](../tools/rl2_q_surface_build.py) / [tools/rl2_q_surface_eval.py](../tools/rl2_q_surface_eval.py) / [tools/rl2_q_surface_figure.py](../tools/rl2_q_surface_figure.py). u→strength: {0.3:small, 0.6:medium, 1.0:large}. safe_utility = `artifact_reduction − α·fidelity_loss` (α=5.0; G2 Protocol B, synthetic Protocol A).

### 8-3. Action-effect surface (정량, mean over states)

**G2 (low severity)** — 모든 tool utility 음수, u 증가 시 악화 (abstain-dominant):

| tool | u=0.3 util | u=0.6 | u=1.0 | viol(u=1.0) |
|---|---|---|---|---|
| BoneProjection | −0.0015 | −0.0037 | −0.0068 | 0% |
| FootLock | −0.0090 | −0.0189 | −0.0308 | 46% |
| VelocitySmoothing | −0.0038 | −0.0142 | −0.0417 | 40% |

**Synthetic severe (high severity)** — VelocitySmoothing utility **양수 + 비단조 (peak at u=0.6)**:

| tool | u=0.3 util | u=0.6 | u=1.0 | viol pattern |
|---|---|---|---|---|
| VelocitySmoothing | +0.0560 (viol 49%) | **+0.0730** (viol 27%) | +0.0261 (viol 2%) | u↑ → safer |
| FootLock | −0.0005 | −0.0105 | −0.0319 | 2→11% |
| BoneProjection | +0.0008 | −0.0059 | −0.0238 | 0→53% |

→ **thesis figure**: synthetic VelocitySmoothing 의 utility 가 intermediate intensity (u=0.6) 에서 peak. u=1.0 은 fidelity-to-clean 개선 (fid −0.025) 에도 artifact_reduction 음수 (−0.101, over-smoothing) 로 utility 하락. **연속 intensity 의 sweet spot 존재** 를 grid 가 포착.

### 8-4. Two-head Q + reranking 평가 (seeds 0/1/2, sample-level split)

**Q heads**: utility R²=**0.485** (MAE 0.0205), **risk AUC=0.913** (acc 0.880). → physical violation 은 (state, tool, u) 에서 매우 예측 가능 (gate-aware filtering 정당화). utility surface 는 학습되나 noisy.

**STOP threshold (ε) calibration** (사용자 Stage 1 goal):

| ε | G2 q_rerank NetGain | G2 STOP | synthetic q_rerank NetGain | synthetic STOP |
|---|---|---|---|---|
| 0.0 | −0.0032 | 39% | +0.1045 | 0% |
| 0.005 | −0.0018 | 76% | +0.1053 | 0% |
| 0.01 | −0.0012 | 85% | +0.1088 | 4% |
| **0.02** | **−0.0006** | 93% | **+0.1087** | 9% |
| (ref) imitation HGB | +0.0004 | 81% | +0.1258 | 0% |
| (ref) safe oracle | +0.0021 | — | +0.2067 | — |

**q_rerank violation rate = 0% in BOTH regimes** (predicted risk filter + real gate re-check, §5-2-6).

### 8-5. 파악한 부분

1. **risk head 가 강력 (AUC 0.913)** — Q surface 의 gate-aware filtering 이 작동. q_rerank 가 두 regime 모두 0% violation 달성 (예측 filter + 실제 gate 재검증).
2. **STOP threshold ε≈0.02 가 regime-adaptive abstention 을 distribution tag 없이 달성**: G2 utility 가 near-zero (noise) 라 ε 가 false-positive correction 억제 → 93% STOP (≈ imitation 81%); synthetic utility 는 large (+0.05~+0.07) 라 ε 통과 → 0% violation 으로 +0.109 복원. **utility magnitude 자체가 regime 을 encode**.
3. **Stage 1 목표 충족**: 기존 3-level 결과를 Q/reranking formulation 으로 재현 (✓), STOP threshold calibration (✓), action-effect surface 의 continuous sweet spot 포착 (✓).

### 8-6. 정직 보고 (AGENTS.md §3-13) — imitation 대비 raw NetGain

calibrated ε=0.02 에서도 **imitation HGB (3-level, 현 PRIMARY) 가 q_rerank 보다 raw NetGain 우위**: G2 +0.0004 vs −0.0006, synthetic +0.1258 vs +0.1087. 이유: imitation 은 oracle 의 선택 action 을 직접 clone 하지만 q_rerank 는 utility surface (R²=0.485 noise) 를 재구성 후 argmax — Stage 1 (3-point grid) 에서는 imitation 의 직접 모방이 더 정확.

**Q surface 의 연구 가치** 는 Stage 1 의 raw NetGain 우위가 아니라: (a) explicit risk prediction (AUC 0.913) 기반 principled gate filtering, (b) interpretable action-effect surface (§8-3 sweet spot), (c) **continuous u (Stage 2-3) 의 토대** — imitation 은 discrete class 라 continuous argmax 불가. Stage 1 은 formulation 재현 + calibration 단계이며 imitation 을 raw 성능 으로 넘어서는 것은 Stage 2+ (dense grid → continuous) 목표.

### 8-7. 한계 (caveat)

- **safe_utility = Category C** internal routing reward (외부 공개 최종 성능 근거 금지, metric_provenance §4-1-1).
- synthetic n=18/seed eval (small). G2/synthetic 모두 controlled diagnostic + real-distribution 혼합.
- utility R²=0.485 (noisy) — Stage 2 dense grid 가 surface smoothness 를 확인해야 continuous argmax (Stage 3) 안정성 보장.
- state set = oracle safe_best prefix (imitation 과 동일 분포) — off-policy state coverage 는 Stage 2+ 확장 대상.

### 8-8. Intent-Reconciliation (AGENTS.md §3-23)

- **Intent**: 사용자 directive 2026-05-29 — RL-2 를 discrete classification 에서 `Q_safe(s, tool, u)` bounded continuous action-effect surface 로 전환, Stage 1 (u_grid={0.3,0.6,1.0}) 구현. 목표 3: 3-level 재현 / imitation 대비 설명력 / STOP calibration.
- **Acceptance criteria**: (state, tool, u, effect) dataset + two-head Q (utility+risk) + reranking closed-loop (real gate 재검증) + imitation 비교 + STOP threshold sweep. governance 등록 (§5-2, §4-1-1).
- **Actual outcome**: dataset 603×9, risk AUC 0.913 / utility R² 0.485, regime-adaptive abstention (ε=0.02), 0% violation. surface sweet spot figure. governance 등록 완료. **단 imitation raw NetGain 우위 (§8-6 정직 보고)** — Stage 1 은 formulation 재현 단계.
- **Verdict**: **`aligned`** (Stage 1 목표 3 충족 + governance + 정직 보고). Stage 2 (dense grid) 가 다음.

---

## 9. RL-2 Stage A — Action-Effect Transition Dataset (dense u-grid, decision-boundary coverage)

### 9-1. 사용자 directive 박제 (데이터 우선순위 재정의)

> "지금 부족한 건 motion sample 이 아니라 action-effect transition sample 이다. 우리가 학습
>  하려는 것은 motion distribution 이 아니라 Q_safe(s, tool, u). MotionGPT 는 state 를 만들고,
>  tool application 이 transition 을 만든다. 좋은 데이터셋은 random motion 많이가 아니라
>  decision boundary 를 덮는 state-action-outcome dataset. Stage A: G2 natural 300 + synthetic
>  severe 60 + clean 100 + near-boundary 50, × 3 tools × 11 u-grid, horizon 1."

논문 framing (단일 출처): "Rather than learning from motion samples alone, ArtifactRouter learns from action-effect transitions obtained by applying candidate tools at bounded intervention intensities and measuring artifact reduction, physical validity, and fidelity."

### 9-2. 산출물

| 산출물 | 경로 |
|---|---|
| transition dataset (raw, **gitignored**) | `evals/snapshots/rl2_transition_dataset_stageA_v1.json` (16,830 transition, 28MB; builder deterministic 재현) |
| compact surface summary (committed) | [evals/snapshots/rl2_transition_surface_stageA_v1.json](../evals/snapshots/rl2_transition_surface_stageA_v1.json) |
| analysis | [evals/snapshots/rl2_transition_analysis_stageA_v1.json](../evals/snapshots/rl2_transition_analysis_stageA_v1.json) |
| curve figure | [reports/figures/2026-05-29/transition_stageA_curves.png](figures/2026-05-29/transition_stageA_curves.png) |

도구: [tools/rl2_transition_build.py](../tools/rl2_transition_build.py) / [tools/rl2_transition_analyze.py](../tools/rl2_transition_analyze.py). 510 state × 3 tool × 11 u = **16,830 transition**. continuous u override (3 tool 에 `continuous_factor`/`continuous_sigma` backward-compatible 추가; u=0 → identity).

### 9-3. State distribution (decision-boundary coverage)

| distribution | n | 역할 | protocol |
|---|---|---|---|
| clean | 100 | STOP / no-harm 경계 | A (vs self) |
| near_boundary | 50 | gate boundary (clean + jitter 0.025, Stage A 조작적 정의) | A |
| g2_natural | 300 | weak correction vs STOP 경계 | B (real-distribution) |
| synthetic_severe | 60 | active multi-step correction | A |

### 9-4. 핵심 발견 (사용자 Stage A 질문 4개 답)

**(1) 각 state type 의 최적 u 가 다르다 (decision-boundary 의미)** — VelocitySmoothing utility peak:

| distribution | peak u | peak utility |
|---|---|---|
| clean | **u=0.0 (STOP)** | +0.000 |
| g2_natural | **u=0.0 (STOP)** | +0.000 |
| synthetic_severe | **u=0.4** | +0.195 |
| near_boundary | **u=1.0** | +0.100 |

→ clean/g2 는 abstain, synthetic 은 intermediate intensity, near_boundary 는 strong. **단일 best u 없음 → conditional intensity selection 의 가치 입증**.

**(2) utility curve smooth** — VelSmooth |2차 차분|max: synthetic 0.077 (peak 곡률), 나머지 0.003~0.025. 대체로 smooth → Stage 3 continuous argmax 가능성 지지.

**(3) gate boundary 가 보인다** — clean VelSmooth violation u=0.2 에서 0.5 교차 (STOP 경계 명확), FootLock u=0.1. synthetic/g2 는 tool 별로 다른 위치.

**(4) tool 별 u sensitivity 가 다르다** — utility range: VelSmooth synthetic 0.195 / near_boundary 0.100 (민감), BoneProjection 0.007~0.037 (둔감), FootLock 0.016~0.036 (중간).

**(5) B2 failure 가 u curve 에서 재현 (가장 중요)** — VelocitySmoothing @ u=1.0 의 dominant physical violation:

| distribution | dominant violation @ u=1.0 |
|---|---|
| **clean** | **BoneLengthCVEvaluator (100%)** |
| g2_natural | BoneLengthCVEvaluator (40%) |
| synthetic_severe | BoneLengthCVEvaluator (3%, jitter 가 지배라 낮음) |

→ **F-5 의 "B2 violation 100% BoneLengthCV" 발견 이 continuous u curve 에서 재현**. clean motion 에 strong smoothing (u=1.0) = 100% BoneLengthCV violation (over-smoothing 이 bone length 왜곡). u 증가에 따른 violation 상승이 BoneLengthCV 에 집중 — over-correction failure mode 의 정량 곡선.

### 9-5. 파악한 부분

1. **transition data 가 핵심** — 510 state 에서 16,830 action-effect transition. motion 수 (510) 보다 transition coverage (16,830) 가 Q_safe 학습의 원재료. MotionGPT generation 보다 tool × u sweep 이 데이터 가치.
2. **decision boundary 가 distribution 별로 명확히 분리** — clean(STOP) / near_boundary(strong) / synthetic(intermediate) / g2(STOP) 의 4 regime 이 utility surface 에서 구분됨.
3. **continuous override backward-compatible** — 기존 token (small/medium/large) 경로 보존, unit 73 passed.

### 9-6. 한계 (caveat)

- **near_boundary = Stage A 조작적 정의** (clean + jitter 0.025). 사용자가 의도한 "near gate-fail boundary hard mining" 은 Stage B (hard-example mining) 대상. 현재는 moderate-corruption proxy.
- horizon=1 (single-step). sequence value 는 Stage C.
- raw transition dataset (28MB) gitignored — builder deterministic 재현 (seed: synthetic 42, select 20260529). surface summary 는 committed.
- safe_utility = **Category C** (metric_provenance §4-1-1). standard metric (FID/R-Prec) 는 transition label 에 미포함 — Stage B+ 검토.

### 9-7. Intent-Reconciliation (AGENTS.md §3-23)

- **Intent**: 사용자 directive 2026-05-29 — action-effect transition dataset builder (motion sample 아닌 transition coverage 우선). Stage A: 4 distribution × 11 u × 3 tool, horizon 1. 목표: utility smoothness / gate boundary / u sensitivity / B2 failure 재현 진단.
- **Acceptance criteria**: (state, tool, u, after_state, gate_result, utility) rich transition schema, decision-boundary 덮는 4 distribution, dense u-grid, continuous tool override.
- **Actual outcome**: 16,830 transition (510 state). 4 질문 모두 답 — 최적 u distribution-dependent, curve smooth, gate boundary 가시, B2 failure (clean u=1.0 100% BoneLengthCV) 재현. governance (continuous mapping §5-2-2, harness_metadata continuous grid) 갱신.
- **Verdict**: **`aligned`** (Stage A 진단 목표 + transition 우선 framing + B2 재현). 단 near_boundary 는 조작적 proxy (§9-6, Stage B hard-mining 으로 보강 예정).
