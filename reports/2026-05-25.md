# 2026-05-25 — Step RL-0 Multi-step Headroom (Sequence Oracle vs B2, synthetic multi-artifact)

> 일자: 2026-05-25.
> 대상 가설: [H-2026-204](../evals/hypotheses/H-2026-204.md) (RQ1+RQ2 — artifact-conditioned tool selection + closed-loop refinement 의 fixed post-processing 대비 NetGain 우위) **RQ2 upper bound** + [H-2026-205](../evals/hypotheses/H-2026-205.md) Stage 0 → Stage 1 (multi-step value learning) transition motivation.
> 트랙: 사용자 directive (2026-05-24) 의 **분기점 진단** — "multi-step RL 로 바로 가지 말고 먼저 headroom 확인. B2 를 넘는 action sequence 가 실제로 존재하는가?"

---

## 1. 진행사항 (이유와 연결)

| 항목 | 이유 |
|---|---|
| Step RL-0 sequence oracle 도구 작성 ([`tools/oracle_sequence_multi_run.py`](../tools/oracle_sequence_multi_run.py)) | 사용자 directive 의 핵심 분기점 ("RL 가치 있나, 아니면 tool/metric 문제인가") 진단. 기존 `oracle_sequence_run.py` 는 single-artifact + single-target 만 지원 — multi-artifact + full-evaluator (target = mean(all 3) + cross-evaluator effect 박제) 확장 필요. |
| DFS exhaustive search (max_depth K=5, AGENTS.md §3-4 일관 pruning) | K=5 = refinement_loop default = fair upper bound. pruning = Score 비감소 + same-pair strength decay (large → medium → small only, cycle 차단). |
| 두 NetGain metric 동시 박제 (NetGain_A = mean(foot+jitter, B2 multi 와 same), NetGain_full = mean(all 3)) | B2 multi 와 paired 비교 가능 + 사용자 directive 의 "full evaluator" 동시 충족. AGENTS.md §6-12 (cross-evaluator side effects) 박제. |
| n=30 (seed=42, sorted glob — B2 multi_v1 same split) 실행 | paired 비교 의 sample-level 일치. |
| Paired test ad-hoc 도구 ([`tools/_paired_test_sequence_oracle_vs_b2.py`](../tools/_paired_test_sequence_oracle_vs_b2.py)) | 사용자 directive 의 정확한 답 (B2 를 넘는가) 의 정량 evidence. |
| 부록 M 작성 (eval-compare 5단계 형식) | H-2026-204 RQ2 upper bound supports 박제. multi-step RL 진입의 정식 motivation 박제. |
| B7 framing 교정 의무 (사용자 directive 박제) | "B7 = single-step RL-style policy / contextual-bandit baseline", NOT "RL". 진짜 RL = trajectory-level value learning 이후. |

---

## 2. 파악한 부분 — 주요 결과

### 2-1. 사용자 directive 의 분기점 정답 — **"B2 를 넘는 sequence 가 실제 존재"**

**Paired test (n=30 common, NetGain_A, B2 multi-compatible)**:

| Metric | Sequence Oracle (K=5) | B2 multi | Δ (oracle - B2) |
|---|---|---|---|
| NetGain median | **+0.314** | +0.180 | **+0.144** |
| NetGain mean | **+0.303** | +0.179 | +0.124 |
| n_strict_oracle / n_tie / n_loss | **30 / 0 / 0** (clean separation) | | |
| **Wilcoxon p (one-sided, oracle > B2)** | **9.3e-10** | | |
| **Cohen's d (paired)** | **+1.71** (very large) | | |
| Bootstrap CI median | [+0.100, +0.168] | | (no zero crossing) |
| Bootstrap CI mean | [+0.097, +0.148] | | (no zero crossing) |

→ **30/30 sample 모두 oracle strict > B2**. action space 의 천장 아님. **Multi-step RL 진입 정당한 motivation 확립**.

### 2-2. Search mechanism 의 정량 발견

- **best_length=5 dominant (16/30 = 53%)** — K=5 가 binding constraint. multi-artifact 의 본격 회복 에는 평균 5 step 필요.
- **first_action=FootLockTool 29/30 (97%)** — foot floating 이 multi-artifact 의 first priority.
- **Search complexity**: per sample ~2600 paths explored (range 1500-4500). Pruning (Score 비감소 + same-pair strength decay) 가 worst case 10^5 = 100K → ~수천 paths 로 축소.
- Same-pair strength decay 가 sequence 의 핵심: 큰 strength 로 시작 → 점차 감소 (예: FL/large → FL/medium → FL/small) → 정밀 회복. single-step 은 본 ramping 자체가 불가.

### 2-3. B7 framing 교정 박제

사용자 directive (2026-05-24) 인용:
> "B7 = contextual bandit baseline. B7 을 'RL' 이라고 단독 표현하지 않기. 'single-step RL-style policy' 또는 'contextual-bandit baseline' 으로 표기. 진짜 RL 은 trajectory-level value learning 이후부터 사용."

본 일지 (2026-05-25) 부터 정정 적용:
- **B7 = contextual-bandit baseline = single-step RL-style policy**. NOT trajectory-level RL.
- 본 프로젝트의 보고서 (2026-05-20, 2026-05-21 등) 는 이미 "B7 bandit" / "contextual bandit" 표기 일관 (grep 확인) — 본 directive 와 충돌 없음.
- "Multi-step RL" (부록 K-10, L-8 의 future work) = **Trajectory-level value learning** (Q-learning, value iteration, sequence-oracle imitation). 본 부록 M 의 +0.124 mean gap 이 정식 motivation.

---

## 3. 실험 메타 (재현)

### 명령

```
# Sequence oracle (Step RL-0, n=30, K=5)
python -m tools.oracle_sequence_multi_run \
    --n-samples 30 --seed 42 --max-depth 5 \
    --task-id oracle_sequence_multi_v1 \
    --split-id multi_artifact_v1 \
    --raw-output-dir evals/raw \
    --output evals/snapshots/oracle_sequence_multi_v1.json

# Paired test
python -m tools._paired_test_sequence_oracle_vs_b2 \
    evals/snapshots/oracle_sequence_multi_v1.json \
    evals/snapshots/baseline_b2_fixed_smoothing_multi_v1.json
```

### 환경

- conda env: `motion3d` (numpy 2.2.6, scipy 1.15.3, sklearn 1.7.2, torch 2.11.0+cu128).
- AGENTS.md §2-1 의 권장 env 명 `motion-router` 와 실제 사용 env `motion3d` 의 불일치는 별도 명시 (env mapping은 reproducibility-checklist 의 보조 항목).
- Synthetic multi-artifact recipe: `inject_foot_floating(lift_height=0.08, seed=42) → inject_jitter(noise_std=0.05, seed=1042)`.
- HumanML3D new_joints 의 sorted glob, np.random.default_rng(42).choice(len, 30) — B2 multi_v1 과 same split.

### 시각화

본 일지에는 시각화 미첨부 (sequence oracle 의 본질이 정량 분포 비교 — 다중 sample paired test). 본격 시각화는 multi-step RL 진입 후의 tool call trace + before/after motion GIF 단계.

---

## 4. 실패한 시도 / negative result

- 없음. paired test 첫 실행 시 trial_id key mismatch (`per_sample` vs `results` field) → 즉시 fix, 재실행 성공.

---

## 5. 결정적 finding (사용자 directive 의 정답)

| Outcome | 본 일자 결과 | 다음 |
|---|---|---|
| ~~Sequence oracle ≈ B2 또는 < B2~~ | (해당 없음) | (tool/reward 재설계 회피) |
| **Sequence oracle > B2** | **+0.124 mean gap, p=9.3e-10, d=+1.71 (very large), 30/30 strict** | **Multi-step RL (trajectory-level value learning) 진입 정당한 motivation 확립** |

**사용자 framing 의 정확한 학술적 의미**:
> "지금 문제는 꽤 치명적일 수 있습니다. 하지만 치명적인 실패라기보다는, '우리가 아직 RL 을 한 게 아니라 single-step bandit 까지만 했다' 는 연구 framing 교정 문제입니다. 다음은 반드시 sequence oracle 로 multi-step headroom 부터 확인하는 게 맞습니다."

→ 본 일자에서 정확한 정답 박제: 정책이 **천장에 못 도달**, action space 충분, multi-step learning 필요.

---

## 6. Raw record cross-link

### Snapshots
- [`evals/snapshots/oracle_sequence_multi_v1.json`](../evals/snapshots/oracle_sequence_multi_v1.json) (n=30, K=5, NetGain_A + NetGain_full).
- [`evals/snapshots/_paired_seq_oracle_vs_b2_multi.json`](../evals/snapshots/_paired_seq_oracle_vs_b2_multi.json) (paired test result).

### Raw records
- `evals/raw/*oracle_sequence_multi_v1*.json` — n=30 per-sample (AGENTS.md §3-15 metadata: severity_versions, split_id=multi_artifact_v1, evaluator_config_hashes, oracle_type=sequence).

### Tools
- [`tools/oracle_sequence_multi_run.py`](../tools/oracle_sequence_multi_run.py) (신규)
- [`tools/_paired_test_sequence_oracle_vs_b2.py`](../tools/_paired_test_sequence_oracle_vs_b2.py) (신규 ad-hoc)

### 5단계 비교 리포트
- [`evals/reports/2026-05-19_h_2026_205_stage0.md`](../evals/reports/2026-05-19_h_2026_205_stage0.md) **부록 M** (Step RL-0 multi-step headroom).

### 이전 일지
- [`reports/2026-05-23.md`](2026-05-23.md) (Step 5-A + 5-B + 5-B v2 G2 small-calibration 정식화).
- [`reports/2026-05-21.md`](2026-05-21.md) (Step 5 G2 transfer diagnostic).
- [`reports/2026-05-20.md`](2026-05-20.md) (Step 1-4 + 3.5 종합).

---

## 7. 예정사항

| Order | Step | 의미 |
|---|---|---|
| 1 | **Step RL-0 보강 — G2 natural 의 sequence oracle 측정** | 사용자 directive 의 "synthetic multi-artifact + G2 natural 모두" 의 G2 부분. low severity 분포에서도 multi-step headroom 존재 여부. |
| 2 | **Step RL-1 — Multi-step value learning** | **+0.124 mean gap 의 정당한 RL motivation**. Q-learning / value iteration / sequence-oracle imitation 중 선택. |
| 3 | Perceptual rating | NetGain proxy 의 perceptual validity (Step RL-1 후보 확정 후). |
| (선택) | K=10 oracle (extended depth) | K=5 binding (53% best_length=5) 의 추가 headroom 진단. |

### 가설 status 갱신 (정식화)

- **H-2026-204 RQ2 (closed-loop > single-step)**: **upper bound supports** — sequence oracle 의 fair upper bound 가 B2 fixed smoothing 대비 +0.124 mean gap. status 전환 (active → supported) 은 AGENTS.md §3-11 사용자 승인 후.
- **H-2026-205 Stage 0 → Stage 1 motivation**: Stage 0 (single-step supervised + bandit) 의 ceiling 진단 완료. Stage 1 (trajectory-level value learning) 진입 정식 motivation 확립.
- **H-2026-206 (generator-agnostic)**: 본 일자 측정 없음. 부록 L (Step 5-B v2) 의 robust evidence 유지.

### 의무 회복 적용 확인

- AGENTS.md §3-6-1 일지 작성 의무: 본 일자 측정 직후 즉시 일지 작성 ✓.
- AGENTS.md §3-16 oracle_type 명시 의무: raw record + snapshot 의 `oracle_type="sequence"` 박제 ✓.
- AGENTS.md §6-12 cross-evaluator side effects: `cross_evaluator_delta` (모든 3 evaluator) 박제 ✓.
- AGENTS.md §6-13 integration smoke ≠ 가설 근거: 본 결과는 trial=30 + paired Wilcoxon + bootstrap CI + effect size 충족 (eval-compare SKILL §6) ✓.

---

## 8. 후속 작업 (같은 일자) — Step RL-0 보강 G2 + K=10 (사용자 roadmap Order 1, 2)

> 본 절은 사용자 directive (2026-05-25 본 일자 후속): "Order 1: G2 natural sequence oracle K=5. 가능하면 K=10 subset. 이렇게 진행하자." — 부록 N evidence 정식화.

### 8-1. 진행사항

| 항목 | 이유 |
|---|---|
| G2 sequence oracle 도구 작성 ([`tools/oracle_sequence_g2_run.py`](../tools/oracle_sequence_g2_run.py)) | 기존 `oracle_sequence_multi_run.py` 는 synthetic injection 기반. G2 natural 은 inject 없음 + Protocol B simplified (reference = original_g2). 별도 도구 작성. |
| B2 G2 v2 재실행 (n=50) | 기존 B2 G2 v1 (n=30) 과 paired cohort 일관화. G2 batch 가 n=50 으로 확장된 후 baseline 갱신. |
| G2 sequence oracle n=50 K=5 실행 (~30-40분) | 사용자 directive Order 1 의 정식 측정. |
| G2 paired test ([`tools/_paired_test_g2_sequence_vs_b2.py`](../tools/_paired_test_g2_sequence_vs_b2.py)) | sequence oracle vs B2 G2 v2 paired Wilcoxon + Cohen's d + bootstrap CI. |
| K=10 subset n=10 실행 (synthetic, 사용자 Order 2) | K=5 binding 진단. 부록 M 의 best_length=5 dominant (53%) 가 단순히 K=5 cap 인지 확인. |
| 부록 N append + 일지 본 절 update | 5단계 형식 evidence 박제. |

### 8-2. 파악한 부분 — 주요 결과

**G2 sequence oracle vs B2 G2 (n=50 paired)**:

| Metric | G2 Sequence Oracle (K=5) | B2 G2 v2 | Δ (oracle - B2) |
|---|---|---|---|
| NetGain median | **0.000** | -0.0129 | **+0.0152** |
| NetGain mean | +0.0110 | -0.0169 | +0.0280 |
| **n_strict_oracle/50** | **50 (100%)** | | clean separation |
| **Wilcoxon p (one-sided)** | **8.9e-16** | | overwhelming |
| **Cohen's d (paired)** | **+0.726** (medium-large) | | |
| Bootstrap CI median (Δ) | [+0.0101, +0.0186] | | (no zero crossing) |

**G2 sequence oracle 의 best-path mechanism (synthetic 과 정반대)**:

| Metric | G2 (n=50) | Synthetic (부록 M, n=30) |
|---|---|---|
| **best_length=0 (STOP)** | **27/50 (54%)** | 0/30 (0%) |
| best_length=1 | 21/50 (42%) | 0/30 |
| best_length=2 | 2/50 (4%) | 0/30 |
| best_length=5 | 0/50 (0%) | **16/30 (53%)** |
| First action | **STOP 27**, FL 14, BP 8, VS 1 | **FL 29**, VS 1 |
| First strength (non-STOP) | small 15, medium 8, **large 0** | predominantly large |

→ **G2 sequence oracle 의 우위 = STOP (abstain) 선택의 효과**, multi-step 의 효과 아님.

**K=10 subset (synthetic, n=10 full)**:

| best_length | 빈도 (n=10) |
|---|---|
| 2 | 1 (10%) |
| 4 | 2 (20%) |
| 5 | 5 (50%) |
| **6** | **2 (20%)** |
| ≥7 | 0 (0%) |

- best_length ≥ 6: 20% — K=5 가 일부 binding, 단 추가 headroom marginal (~+0.01-0.02 mean).
- → **RL-1 의 effective K=5 적절**.

### 8-3. 결정적 finding — distribution-specific oracle pattern

**핵심 finding** (사용자 directive 의 분기점 종합):

| Distribution | sequence oracle vs B2 | best-path mechanism | RL-1 motivation |
|---|---|---|---|
| **Synthetic multi-artifact** (high severity inject) | Δ median +0.144, d=+1.71 (very large), 30/30 strict | **5-step dominant** (53%), large→small decay sequence | **Multi-step RL 강력하게 가치 있음** (부록 M) |
| **G2 natural** (low severity, MotionGPT pretrained) | Δ median +0.015, d=+0.726 (medium-large), 50/50 strict | **STOP dominant** (54%), 1-step minimal | **Abstain learning 이 multi-step 보다 중요** (본 부록 N) |

→ **RL-1 의 정확한 form** = "sequence oracle imitation + abstain learning" (STOP 도 action). 사용자 directive 의 sequence oracle imitation 권장과 일치.

### 8-4. B7 framing 교정 / RL framing 정밀화 (사용자 directive 박제)

- 부록 N-4 가설 평가 절에서 framing 정식화: **RL-1 의 form 은 distribution 별로 다름**.
- Synthetic 영역: **trajectory-level value learning** (Q-learning 또는 sequence-oracle imitation).
- G2 영역: **abstain learning** (state → STOP vs revise decision).
- 사용자 권장: sequence oracle imitation 우선 (이미 oracle data 확보, Q-learning 보다 안정적).

### 8-5. 실패한 시도 / negative result

- 없음. paired test trial_id matching 에서 B2 G2 v1 (n=30) vs sequence oracle (n=50) cohort 차이 발견 → B2 G2 v2 (n=50) 재실행으로 해소.

### 8-6. Raw record cross-link (8절 추가)

- [`evals/snapshots/oracle_sequence_g2_v1.json`](../evals/snapshots/oracle_sequence_g2_v1.json) (n=50 G2 sequence oracle).
- [`evals/snapshots/baseline_b2_fixed_smoothing_g2_v2.json`](../evals/snapshots/baseline_b2_fixed_smoothing_g2_v2.json) (B2 G2 v2 재실행).
- [`evals/snapshots/_paired_g2_seq_oracle_vs_b2.json`](../evals/snapshots/_paired_g2_seq_oracle_vs_b2.json) (G2 paired).
- [`evals/snapshots/oracle_sequence_multi_k10_subset.json`](../evals/snapshots/oracle_sequence_multi_k10_subset.json) (K=10 subset n=10).
- [`tools/oracle_sequence_g2_run.py`](../tools/oracle_sequence_g2_run.py) (신규).
- [`tools/_paired_test_g2_sequence_vs_b2.py`](../tools/_paired_test_g2_sequence_vs_b2.py) (신규).
- `evals/raw/*oracle_sequence_g2_v1*.json` (n=50, AGENTS.md §3-15 metadata).
- `evals/raw/*oracle_sequence_multi_k10_subset*.json` (n=10).

### 8-7. 5단계 비교 리포트 cross-link (8절 추가)

- [`evals/reports/2026-05-19_h_2026_205_stage0.md`](../evals/reports/2026-05-19_h_2026_205_stage0.md) **부록 N** — Step RL-0 보강 G2 + K=10.

### 8-8. 예정사항 (정밀화 — 사용자 roadmap 진행 갱신)

| Order | Step | v2 후 status |
|---|---|---|
| 1 | G2 natural sequence oracle K=5 | **완료** (본 후속). |
| 2 | K=10 subset (K=5 binding 진단) | **완료** (n=10 subset, K=5 dominant 적절 확인). |
| 3 | **RL-1: sequence oracle imitation** | **다음 우선순위**. (state, action) supervised 학습. STOP 도 action. synthetic + G2 oracle data 모두 사용. |
| 4 | RL-2: Q-learning / value iteration | imitation 이 ceiling 못 따라가면 진행. |
| 5 | Perceptual rating | 최종 후보 policy 후 perceptual validity. |

### 8-9. H-2026-203 (No-harm) 의 정량 motivation 확립

본 G2 sequence oracle 결과의 **STOP-best 54%** 는 [H-2026-203](../evals/hypotheses/H-2026-203.md) (High-quality motion 의 abstain 능력) 의 정량 baseline. abstain learning 의 가치 정식 확립 — RL-1 의 핵심 component.

---

## 9. 후속 작업 (같은 일자) — RL-1 Sequence-Oracle Imitation Policy 첫 prototype

> 본 절은 사용자 directive (2026-05-25 본 일자 후속): "다음은 RL-1 으로 가는 게 맞습니다. State 는 (current scores + score delta + step index + previous tool + previous strength + remaining budget) 16-dim. 4 goals: ① synthetic sequence ② G2 STOP ③ B2/B5/B6/B7 대비 NetGain ④ oracle gap closure. 이렇게 진행해줘."

### 9-1. 진행사항

| 항목 | 이유 |
|---|---|
| RL-1 training data builder ([`tools/rl1_build_imitation_data.py`](../tools/rl1_build_imitation_data.py)) | oracle best path 를 re-simulate 하여 (state_16, action_10) pair 추출. 부록 M (synthetic) + 부록 N (G2) 양쪽 사용. |
| SequenceImitationSelector module ([`orchestrator/sequence_imitation.py`](../orchestrator/sequence_imitation.py)) | sklearn RF 10-class joint classifier. 사용자 directive 의 16-dim state + 10-action space 정확 구현. |
| Closed-loop runner ([`tools/baseline_rl1_run.py`](../tools/baseline_rl1_run.py)) | per distribution NetGain rollout. AGENTS.md §3-4 Score 비감소 violation → rollback + STOP. |
| Paired test ad-hoc ([`tools/_paired_test_rl1_vs_baselines.py`](../tools/_paired_test_rl1_vs_baselines.py)) | 4 goals 의 정량 evidence. RL-1 vs B2 + oracle gap closure ratio. |
| 부록 O append + 일지 본 절 update | 5단계 형식 + 4 goals 평가 박제. |

### 9-2. 파악한 부분 — 주요 결과

**Training data**:
- 232 (state, action) pair = synthetic 157 + G2 75.
- Action distribution: **STOP 80 (34.5%)**, VS-decay (large 22 + medium 24 + small 15 = 61), FL (55), BP (36).
- Eval action-prediction accuracy (held-out): 66.7% (n=72 pairs).

**Synthetic eval (n=9 disjoint)**:
- NetGain median +0.020, mean +0.142.
- length=5 도달 3/9 (33%), first_action FL 7/9 (78%).
- **stop_reason score_violation_rollback 5/9 (56%) — sequence 부정확.**

**G2 eval (n=15 disjoint)**:
- NetGain median 0.000, mean +0.0004.
- length=0 (STOP) 10/15 (67%), length=1 5/15.
- **stop_reason selector_stop 15/15 (100%) — abstain learning 완벽.**

**Paired test (사용자 4 goals)**:

| Goal | 평가 | Synthetic | G2 |
|---|---|---|---|
| 1. Multi-step sequence 학습 | partial (synthetic), excellent (G2 STOP) | length=5 도달 33% (oracle 53%), FL first 78% | STOP first 67%, selector_stop 100% |
| 2. STOP 학습 (abstain) | excellent in G2 | n/a | first STOP 67% (oracle 54%) |
| 3. NetGain vs B2 | G2 win / **Synthetic sub-B2** | Δ med -0.137, **5/9 loss**, d=**-0.26** | Δ med +0.013, **15/15 strict**, d=**+1.48**, p=3.1e-05 |
| 4. Oracle gap closure | G2 거의 ceiling / Synthetic negative | **-40%** (negative closure) | **92.1%** (excellent) |

→ **distribution-specific learning capacity 발견**:
- **G2 (abstain learning)**: 매우 성공 — 16-dim state 의 power.
- **Synthetic (multi-step sequence learning)**: 부분 실패 — score_violation_rollback 56% 가 한계.

### 9-3. 결정적 finding — 사용자 framing 정확한 박제

**G2 측면**: RL-1 sequence-oracle imitation policy 가 **abstain learning** 정식 구현 + 검증 완료 (Cohen's d=+1.48 vs B2, 92% oracle closure). H-2026-203 (No-harm) 의 정식 supports evidence.

**Synthetic 측면**: RL-1 imitation 만으로는 **oracle path 의 정확한 decay sequence (large → medium → small) 학습 부족** (-40% closure, sub-B2). **RL-2 (Q-learning / value iteration) 또는 training data 보강** 의 정식 motivation. 사용자 directive: "imitation 이 ceiling 못 따라가면 RL-2 진행".

### 9-4. B7 / RL framing 정밀화 (재확인)

| 표현 | 정확한 의미 |
|---|---|
| **RL-1 = sequence-oracle imitation policy** | 지도학습 sequence imitation. NOT Q-learning. NOT 진짜 RL. |
| B7 = contextual-bandit baseline | single-step RL-style policy. |
| **RL-2 = Q-learning / value iteration** | 진짜 RL — trajectory-level value learning. **synthetic underperformance 시 진입 motivation 확립**. |

### 9-5. 실패한 시도 / negative result

- **Synthetic 의 RL-1 sub-B2 (5/9 loss)** — **정식 negative result 박제**. RL-1 imitation 만으로 synthetic multi-step sequence 의 완전 학습 불가. RF + 16-dim state + 21 train sample 의 capacity 한계 (또는 training data 보강 필요).
- score_violation_rollback 5/9 (56%) — closed-loop oracle path 의 정확한 timing 학습 어려움.

### 9-6. Raw record cross-link (9절 추가)

- [`evals/snapshots/rl1_imitation_training_data_v1.json`](../evals/snapshots/rl1_imitation_training_data_v1.json) (232 pairs).
- [`evals/snapshots/baseline_rl1_imitation_v1.json`](../evals/snapshots/baseline_rl1_imitation_v1.json) (closed-loop eval).
- [`evals/snapshots/_paired_rl1_vs_baselines.json`](../evals/snapshots/_paired_rl1_vs_baselines.json) (paired test).
- [`tools/rl1_build_imitation_data.py`](../tools/rl1_build_imitation_data.py) (신규).
- [`orchestrator/sequence_imitation.py`](../orchestrator/sequence_imitation.py) (신규 module).
- [`tools/baseline_rl1_run.py`](../tools/baseline_rl1_run.py) (신규 runner).
- [`tools/_paired_test_rl1_vs_baselines.py`](../tools/_paired_test_rl1_vs_baselines.py) (신규 paired test).
- `evals/raw/*baseline_rl1_imitation_v1*.json` (24 records, synthetic 9 + G2 15, AGENTS.md §3-15 metadata).

### 9-7. 5단계 비교 리포트 cross-link (9절 추가)

- [`evals/reports/2026-05-19_h_2026_205_stage0.md`](../evals/reports/2026-05-19_h_2026_205_stage0.md) **부록 O** — RL-1 sequence-oracle imitation policy 첫 prototype.

### 9-8. 예정사항 (사용자 roadmap 정밀화)

| Order | Step | Status |
|---|---|---|
| 3 | RL-1: sequence oracle imitation | **completed (first prototype)** — G2 success, synthetic partial. |
| 3.5 | **시각화 sanity check** (사용자 directive deferred) | **다음 후속**. synthetic: foot/jitter visual 감소. G2: STOP-best sample 의 B2 강제 시 over-modification. |
| 3.6 | RL-1 multi-seed + training data 보강 | synthetic underperformance variance / capacity 진단. |
| 4 | RL-2: Q-learning / value iteration | RL-1 의 synthetic ceiling 못 따라잡음 (oracle gap -40%) — 정식 motivation 확립. |
| 5 | Perceptual rating | 최종 후보 policy 후. |

### 9-9. 의무 회복 적용 확인

- AGENTS.md §3-6-1 일지 작성 의무: 같은 일자 후속 절 추가 ✓.
- AGENTS.md §3-13 Negative result 보존: synthetic RL-1 sub-B2 정식 박제 ✓.
- AGENTS.md §3-15 raw record metadata: 24 records 의 severity_versions, split_id, evaluator_config_hashes ✓.
- AGENTS.md §6-13 integration smoke ≠ 가설 근거: trial ≥ 9 (synthetic) + 15 (G2), paired Wilcoxon, Cohen's d 충족 ✓.
