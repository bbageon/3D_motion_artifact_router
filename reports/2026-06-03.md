# 2026-06-03 — AR-037 Effect-Aware Ranker State Ablation (v0/v1/v2 schema 고정)

> Scope: AR-037 — Effect-Aware Quality-Gain Ranker 의 관측 state 를 v0/v1/v2 로 고정 + before-action observable 검증 + dimension 보고. **Claim Boundary (명세): 정책 성능 증명 아님, state 설계 고정 (q_proxy/effect-label/oracle 은 다음 작업).**

## 1. 명세 (single source)

[.claude/docs/dashboard-task-specs/AR-037-effect-aware-state-design.md](../.claude/docs/dashboard-task-specs/AR-037-effect-aware-state-design.md). 핵심 원칙: (1) state(적용 전)/label(적용 후) 분리 — 본 작업은 state 만. (2) v0=global quality-aware (target-aware 아님), v1+=target-aware. (3) raw embedding 직접 투입 금지 → similarity/distance/confidence scalar 만. (4) FID/R-Prec/MM-Dist=최종 validation, 단 per-sample scalar 는 state 후보.

## 2. 산출물

- 도구: [tools/effect_aware_state.py](../tools/effect_aware_state.py) (schema 단일 출처 + builder) · [tools/effect_aware_state_verify.py](../tools/effect_aware_state_verify.py) (검증 runner).
- snapshot: [evals/snapshots/effect_aware_state_schema_v0.json](../evals/snapshots/effect_aware_state_schema_v0.json) (고정 schema + dim + observability audit + 30 G2 motion build 검증).

## 3. 고정 schema + dimension (성공 조건 #1·#3)

| version | 이름 | state dim | state+action | block |
|---|---|---|---|---|
| **v0** | global quality-aware ranker | **40** | 50 | motion 14 + artifact 7 + physical 7 + semantic 4 + history 8 |
| **v1** | target-aware quality-gain ranker | **64** | 74 | v0 + local_target 16 + relation 8 |
| **v2** | support-aware constrained ranker | **71** | 81 | v1 + data_support 7 |
| action | (tool,target,u,stop) | — | 10 | tool 3 + target 3 + strength 3 + stop 1 |

**정직 보고 (§3-13)**: 명세의 stated target range (v1 70-95, v2 80-110) 보다 actual (64/71) 이 약간 낮음. 명세의 **explicit feature list 합** 이 authority (range 는 추정치). AR-037 성공 조건은 dim 목표가 아니라 **schema 고정**이므로 actual dim 을 그대로 보고·고정.

## 4. before-action observable 검증 (성공 조건 #2)

- **금지 feature 배제 확인**: `sample_id`·`trial_id`·`pool_dir`·`split`·`gate_result`·`quality_after`·`quality_gain`·`after_state`·`dense_oracle_action`·final standard metric 등 → schema 에 0건 leak (`forbidden_leaked=[]`).
- **after-action 휴리스틱 audit**: suspicious 0건. 단 `previous_accepted_gain`(history — 직전 step gain, 현재 action 전 관측) + `ensemble_gain_variance`(uncertainty — ensemble 예측 분산, 적용 후 결과 아님)는 `_gain` substring 으로 1차 flag 됐으나 **명세상 before-action observable** 이라 사유 박제 후 통과 (false positive).
- **실제 build (30 G2 motion)**: v0 dim assert 통과. **non-semantic NaN = 0** — motion(14)+artifact(7)+physical(7)+history(8) = 36 feature 전부 결정적 계산 + before-action observable 확인. semantic(4) 만 NaN (mgpt tm2t precompute 전, schema 위치 고정).

## 5. 미완 (다음 작업으로 명시, schema 는 고정)

- semantic block (text_motion_similarity 등 4): mgpt env tm2t co-embedding 별도 pass 필요 (scalar summary, raw embedding 금지 원칙 준수).
- v1 일부 (local_artifact_score / local_physical_score / local_bone_cv_max + relation 3): local evaluator pass·proxy 계산 미구현 placeholder (schema 위치 고정). 다음 작업.
- v2 data_support (7): kNN/ensemble 기반 → q_proxy·M1 oracle·ensemble 학습 후 계산.

## 6. 파악한 부분

1. **v0 의 36/40 feature 가 motion-router env 만으로 결정적·before-action observable** — schema 가 즉시 사용 가능 (semantic 4 만 cross-env).
2. **금지 feature 0 leak** — state/label 분리 (적용 전/후) 원칙이 schema 레벨에서 강제됨. q_proxy 정의 시 label(적용 후) 은 별도 관리.
3. v0/v1/v2 의 ablation 비교 가능성 (성공 조건 #4): builder 가 동일 motion 에서 결정적 vector 생성 확인. 같은 split/candidate/q_proxy 비교는 q_proxy 정의(다음 작업) 후.
4. target-aware claim 은 v1+ 에서만 (성공 조건 #5): v0 는 target 없이 build, v1 은 sample target 으로 build 확인.

## 7. 다음 작업 (명세 §다음 작업)

1. `q_proxy` 정의 (per-sample reward proxy; FID/R-Prec 는 최종 validation 으로 분리).
2. 기존 transition rows 에 v0 state vector 생성 (+ semantic mgpt pass).
3. v0 M1 dense effect oracle 실행.
4. v1 target proposal + local/relation feature 구현.
5. v0/v1/v2 ablation 으로 M0 대비 g2_stress headroom 검증.

## 8. Intent-Reconciliation (AGENTS.md §3-23)

- **Intent**: AR-037 명세 — Effect-Aware Ranker 의 state 를 v0/v1/v2 로 고정 + before-action observable 검증 + dim 보고. Claim Boundary = 성능 증명 아닌 설계 고정.
- **Acceptance criteria (명세 성공 조건)**: (1) schema 고정 (2) before-action observable 확인 (3) dim 보고 (4) ablation 비교 가능 (5) target-aware=v1+.
- **Actual outcome**: v0/v1/v2 schema 고정 (40/64/71) + action 10. 금지 feature 0 leak. v0 36/40 feature 실제 build 에서 NaN 0 (computable+observable), semantic 4 는 mgpt 다음 pass. v1/v2 일부 placeholder (schema 고정, 계산은 다음 작업) — Claim Boundary 와 일관.
- **Verdict**: **`aligned`** (성공 조건 5개 충족 — schema 고정·observable 검증·dim 보고·ablation 비교 가능 구조·target-aware v1+). 단 dim 이 명세 stated range 하한 약간 미만 (explicit list authority, §3 정직 보고). v1/v2 미계산 feature 는 명세 "다음 작업" 범위.

## 9. 사용자 피드백 반영 (3개 risk 정리)

사용자 directive: AR-037 산출의 3개 risk 정리 (특히 #1 즉시).

| # | risk | 처리 |
|---|---|---|
| **#1 hash() encoding** | 🔴 높음 | `target_type_encoding = hash(...) % 7` 은 PYTHONHASHSEED 비결정성 → **재현성(우선순위 #2) 위반**. **`TARGET_TYPE_ID` 고정 dict (append-only)** 로 교체. **검증**: PYTHONHASHSEED 1/42 모두 `both_feet=2, right_arm=10` 동일 — 비결정성 제거 확인. |
| **#3 v1/v2 placeholder** | 🟡 중간 (부분 해소) | v1 의 `local_artifact_score`/`local_physical_score`/`local_bone_cv_max` (target segment 에 evaluator 적용) + relation `root_target_velocity_mismatch`/`parent_chain_bone_margin`/`contact_phase_consistency` (결정적 proxy) **실제 계산**. **v1_add NaN = 0** 확인. v2 data_support(7) 는 kNN/ensemble 필요 → 다음 작업. |
| **#2 semantic 4개 NaN** | 🟡 중간 (backlog) | text_motion_similarity 등 = mgpt env tm2t co-embedding 필요 (raw embedding 금지, scalar summary). schema 위치 고정, 계산은 별도 mgpt pass → **AR-040 등록**. |

→ #1 완전 해소 (재현성 복구), #3 v1 완전 계산 (v2 data_support 만 다음 작업), #2 backlog. before-action observable 원칙 유지 (모든 신규 계산이 적용 전 motion segment 기반).

## 10. state/action feature namespace 충돌 제거 (사용자 directive)

사용자 지적: `target_type_encoding` 이 v1 state(local_target) 와 action 양쪽에 존재 → dict merge / vectorize 시 충돌 (입력 정의 모호성, 성능 문제 아님).

- 확인: state ∩ action 충돌 = `target_type_encoding` 1건 (정밀 확인).
- 수정: **action feature 전체에 `action_` prefix** (`action_target_type_encoding`/`action_target_side`/`action_u` 등). state block 이름은 spec 그대로 (블록 내 unique). 미래 충돌(`u`, `tool_*` 등)까지 방지.
- **충돌 감지 자가 검증 추가**: `verify_schema()` 가 `state_action_name_collisions` 계산 → `passed` 조건에 포함 (regression 차단). 현재 **collisions = [] , passed = True**. dim 불변 (v0 40/50, v1 64/74, v2 71/81).

### 10-1. 남는 불확실성 (사용자 지적, §3-22 `internal proxy assumption`)

v1 의 local evaluator / relation proxy 는 **internal proxy** — 실제 motion quality (FID/R-Prec/MM-Dist/perceptual) 와의 정렬은 **미검증**:

- `local_artifact_score` / `local_physical_score` / `parent_chain_bone_margin` / `contact_phase_consistency` 가 실제 quality 와 얼마나 맞는지는 **AR-040 이후 (q_proxy + standard metric 대조) 검증 의무**.
- 근거 (사용자 제공, 2020+ top-tier): semantic/naturalness state 후보 = HumanML3D CVPR 2022 / MoMask CVPR 2024 ; v1 relation/local physical = HuMoR ICCV 2021 / PhysDiff ICCV 2023 ; v2 support/uncertainty = CQL NeurIPS 2020 (offline OOD value overestimation).
- 표기: 본 proxy 들은 `internal proxy assumption` — learned policy 의 최종 quality claim 의 단독 근거 금지 (§3-20 Category C 일관).

→ AR-037 은 이제 v0/v1 실험을 시작할 수 있는 **재현 가능·namespace-clean state builder**. proxy-quality 정렬 검증은 다음 작업.
