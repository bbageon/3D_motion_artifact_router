# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
dist/
build/

# Virtual env
venv/
.venv/

# Jupyter
.ipynb_checkpoints/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# PyTorch
*.pt
*.pth
*.ckpt
*.safetensors

# 외부 자산 (본 저장소 내부 실제 복사본, 2026-05-15 마이그레이션 완료)
external_assets/processed_noaug/
external_assets/local_lora_g3/
external_assets/HumanML3D/
external_assets/g2_generated*/

# MotionGPT (G2) — 공식 repo clone (sub-repo, 본 저장소 추적 제외)
external_assets/MotionGPT/

# MotionGPT clean install (W-2026-001 resolution attempt — 2026-05-19)
# 기존 MotionGPT/ 와 별도 위치에 fresh install 하여 4-frame collapse 의 환경 이슈 vs
# 본질 issue 분리. clone + deps + checkpoint 모두 재설치. 추적 제외.
external_assets/MotionGPT_clean/

# MDM / MoMask 공식 repo clone (sub-repo, 추적 제외) + generator 생성 pool (재현 가능, AR-048)
external_assets/motion-diffusion-model/
external_assets/momask-codes/
external_assets/mdm_generated*/
external_assets/momask_generated*/
external_assets/protocol_v1_pool*/

# 학습 산출물 (보통 크기 큼)
checkpoints/
runs/
wandb/
mlruns/

# 실험 산출물 (선택적 — 일지 첨부 GIF는 git 포함, 큰 영상은 LFS)
experiments/**/outputs/
experiments/**/tmp/

# RL-2 action-effect transition raw dataset (대용량, deterministic 재현 가능 via tool)
# compact surface summary 만 git 추적.
evals/snapshots/rl2_transition_dataset_stageA*.json
evals/snapshots/rl2_transition_g2_real_stage2*.json
# Trained Q policy bundles (joblib, deterministic 재학습 가능 via tools/rl2_q_policy_train.py)
evals/models/
# Closed-loop final motions (대용량 npy, Step 6 part 2 standard metric 입력 — 재생성 가능)
evals/snapshots/closed_loop_final_motions_*/

# Log
*.log

# OS
Thumbs.db

# 로컬 환경 변수
.env
.env.local
