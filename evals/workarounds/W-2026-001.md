---
w_id: W-2026-001
status: open
severity: critical
discovered_at: 2026-05-19
registered_at: 2026-05-19
related_hypotheses: [H-2026-204, H-2026-206, H-2026-203]
related_components: [generators/motiongpt_wrapper.py, generators/_motiongpt_inference.py]
---

# W-2026-001 — MotionGPT (G2) length 제어 불가능 (degenerate 4-frame output)

## 발견 경위

[`reports/2026-05-19d.md §7-2`](../../reports/2026-05-19d.md) 의 Action 2 (G2 natural artifact 분포 측정) smoke test 에서 발견. 2 sample 모두 60+ 단어 detailed prompt 사용 + `n_frames=60` hint 에도 불구하고 `motion_001.npy`, `motion_002.npy` 모두 shape (4, 22, 3) 으로 degenerate 4-frame 출력.

## 정공법 시도와 실패

- **n_frames hint 변경**: `batch['length'] = [args.n_frames]` 로 60, 40, 100 시도. 모두 4 frames.
- **Prompt 변경**: 공식 [`external_assets/MotionGPT/demos/t2m.txt`](../../external_assets/MotionGPT/demos/t2m.txt) 의 detailed prompt 사용. 동일 결과.
- **Seed 변경**: seed 42, 43 모두 4 frames.

## 원인 분석

MotionGPT 의 `model.forward(batch, task='t2m')` flow:
1. `batch['length']` 를 capture 만 함 (length conditioning 미적용).
2. 실제 motion token 생성은 LM (T5) 의 `generate_direct(texts, do_sample=True)` 에 위임.
3. T5 의 default `max_new_tokens` (또는 stopping criteria) 가 짧은 token sequence 출력 → motion VQ-VAE detokenization 결과 매우 짧음 (4 frames).

본질적으로 **MotionGPT 의 length 제어 mechanism 자체가 batch['length'] 인자를 effective 하게 사용하지 않음** — generation 은 T5 LM 의 자율적 stopping 에 의존.

## Severity 근거 — **critical**

[H-2026-204](../hypotheses/H-2026-204.md) 의 `track_scope: [G1, G2]` 의 G2 측 검증이 본 issue 로 인해 **불가능**:
- G2 natural artifact 분포 측정 — Action 2 의 deliverable 수행 불가.
- G1 ↔ G2 bidirectional generator transfer ([H-2026-206](../hypotheses/H-2026-206.md)) 검증 — G2 측 데이터 부재로 불가.
- High-quality motion no-harm ([H-2026-203](../hypotheses/H-2026-203.md)) — G2 가 high-quality tier 가 아니라 G1 만 사용 가능하므로 일부 무관.

본 issue 는 **외부 공개 보류 조건** (AGENTS.md §3-14 critical 미해소).

## 해결 방향 후보

### 방안 A — MotionGPT 의 T5 LM generation 직접 호출 (코드 수정)

[`generators/_motiongpt_inference.py`](../../generators/_motiongpt_inference.py) 의 `model.forward(batch, task='t2m')` 호출을 분해해 T5 LM 의 `generate` 를 직접 호출. `min_new_tokens` 또는 `min_length` 인자로 강제. 작업량: 중 (MotionGPT 내부 코드 분석 필요).

### 방안 B — MotionGPT issue tracker 검색 + upstream fix 적용

[OpenMotionLab/MotionGPT issues](https://github.com/OpenMotionLab/MotionGPT/issues) 에서 length control 관련 issue 검색. 공식 fix 가 있으면 적용.

### 방안 C — Alternative G2 후보 generator

T2M-GPT (Zhang et al. CVPR 2023, MotionGPT 의 precursor) 또는 다른 token-based generator wrapping. G2 quality-tier 정의에 부합하면 active scope 유지 가능.

### 방안 D — Action 2 scope 재정의 (단기)

G2 inference blocker 가 해소되기 전까지 Action 2 는 **HumanML3D `new_joints` 의 test split** 을 G2 의 natural distribution proxy 로 일시 사용. 단 strictly G2 natural 이 아니므로 결과는 informational.

## 본 저장소 영향

- AGENTS.md §3-5 (generator quality-tier 분리) 의 G2 측 현재 0 measurement.
- AGENTS.md §6-10 (generator transfer 결과 임의 일반화 금지) — G2 측 미측정 상태에서 H-2026-206 supports/contradicts 단정 금지.
- 본 W-id 가 `open` 상태인 한 외부 공개 결과에 G2 관련 인용 시 critical caveat 동봉 의무.

## 다음 스텝 (resolution_target)

- **단기 (이번 turn)**: 본 W-id 등록 + Action 2 scope 재정의 (방안 D) → 사용자 결정 대기.
- **중기 (다음 1-2 turn)**: 방안 A (MotionGPT T5 LM 직접 호출) 시도 또는 방안 B (upstream issue 검색).
- **장기 (Week 3+)**: 방안 C (T2M-GPT 등 alternative G2) 도입 검토.

resolution 시 본 W-id 의 status `open` → `resolved` 전환은 AGENTS.md §3-11 사용자 승인 게이트 (critical 필수) 거친다.

## 진단 시도 (2026-05-19, 사용자 directive)

사용자 4단계 진단 plan ([reports/2026-05-19e.md](../../reports/2026-05-19e.md) 인용) 진행 결과:

### 1단계 — 공식 demo.py 정상 길이 output 확인 (부분 막힘)

```
cd external_assets/MotionGPT
conda run -n mgpt python demo.py --cfg ./configs/config_h3d_stage3.yaml --example ./demos/t2m_mini.txt
```

결과: **CUDA 미설치 환경 이슈로 직접 비교 불가**.

```
AssertionError: Torch not compiled with CUDA enabled
  File "...\demo.py", line 166, in main
    model.to(device)
```

`mgpt` env 의 torch 가 **`2.12.0+cpu`** (CUDA 없음). 공식 demo.py 는 `cfg.ACCELERATOR == "gpu"` 분기로 무조건 `device = torch.device("cuda")` 호출 — CPU fallback 코드 없음. wrapper 의 [`_motiongpt_inference.py`](../../generators/_motiongpt_inference.py) 는 `if cfg.ACCELERATOR == "gpu" and torch.cuda.is_available()` 로 자체 CPU fallback 있어 동작은 했음 (단 4-frame degenerate output).

### 2단계 핵심 발견 — 공식 demo.py 의 batch['length'] = [0]

공식 demo.py 의 [`load_example_input`](../../external_assets/MotionGPT/demo.py) line 39:

```python
motion_lengths = [0] * len(Lines)
```

→ plain text prompt (`#` 없는 t2m.txt) 의 모든 sample 의 length 가 **0**. 즉 공식 정상 inference 는 `batch['length'] = [0]` 으로 호출.

wrapper 의 기존 `batch['length'] = [args.n_frames=60]` 은 공식 path 와 다른 호출 — 본 차이가 length issue 의 직접 원인 후보.

### 2단계 — length=0 patch 시도 (1차 hang, 2차 결정적 발견)

[`_motiongpt_inference.py`](../../generators/_motiongpt_inference.py) 의 `batch['length']` 를 `[0]` (n_frames=0 일 때) 으로 patch.

**1차 시도** (wrapper subprocess 경유):
```
conda run -n motion3d python -m generators.motiongpt_wrapper \
    --prompt "a person walks forward then sits down on the ground" \
    --n-frames 0 --seed 42
```
결과: **3시간 hang** (CPU 1초만 사용, output 0 lines). Manual stop. 원인 unclear — conda activation lock / subprocess buffering / 일시적 환경 이슈 추정.

**2차 시도** (wrapper subprocess 우회, `_motiongpt_inference.py` 직접 호출 + 절대경로 + `--no-capture-output`):
```
conda run -n mgpt --no-capture-output python <abs path>/generators/_motiongpt_inference.py \
    --motiongpt-root <abs>/external_assets/MotionGPT \
    --cfg configs/config_h3d_stage3.yaml \
    --ckpt <abs>/...motiongpt_s3_h3d.tar \
    --prompt "a person walks forward then sits down on the ground" \
    --n-frames 0 --seed 42 \
    --output <abs>/tmp_smoke/motion_length0.npy
```

**결과 (Runtime ~30초)**:
```
[INFO] batch['length']=[0], prompt[:60]='a person walks forward then sits down on the ground'
[OK] joints (4, 22, 3) (length_generated=4) -> .../motion_length0.npy
```

**결정적 발견**:
- length=0 (공식 demo.py 와 동일 path) 도 **4-frame degenerate output**.
- 이전 wrapper smoke (length=60) 와 동일 결과.
- **wrapper 의 batch['length'] 값을 어떻게 주든 결과 동일** → length issue 의 진짜 원인은 wrapper 의 length parameter 가 아니라 **MotionGPT 내부 T5 LM 의 generate 호출 자체**.
- 1차 hang 은 본 patch 와 무관한 일시적 외부 요인. 2차 시도가 정상 30초 완료.

**Patch rollback**: length=0 / length=60 결과 동일하므로 wrapper 의 batch_length 분기 의미 없음. 원래대로 [`_motiongpt_inference.py`](../../generators/_motiongpt_inference.py) `batch['length'] = [args.n_frames]` 원복.

### 3단계 — checkpoint/tokenizer/config 또는 T5 LM generate 동작 확인

2단계 결정적 발견 후 3단계 진입 방향:

**3a) T5 LM 의 generate parameter 강제** (가장 유력):
- MotionGPT 의 model 내부에서 T5 LM 이 `generate_direct(texts, do_sample=True)` 호출 시 default `max_new_tokens` / `min_new_tokens` 가 짧은 sequence stop 유도.
- `mGPT/models/mgpt.py` 또는 `mGPT/models/base.py` 에서 T5 generation parameters override 위치 식별 + min_new_tokens 강제.

**3b) Checkpoint 로드 시점의 vocab 확장 경고 영향**:
- transformers 메시지: "new embeddings will be initialized from multivariate normal distribution" — vocab 확장 (motion_id_512 등 추가 token) 시 LM head 가 재초기화 됐을 가능성.
- 단 checkpoint state_dict 로드 후 다시 그 부분이 trained weight 으로 덮어지므로 영향 미미할 것. 추가 확인 필요.

**3c) MotionGPT 의 motion VQ-VAE codebook 1-2 token short stopping**:
- LM 이 `<motion_id_codebook_size>` (start) → `<motion_id_codebook_size+1>` (end) 사이의 motion token 을 generate 해야 함. end token 이 너무 빨리 sample 되면 short motion 생성 (4 frames).
- 본 동작은 randomness + sampling temperature 의존. seed 변경 또는 sampling 옵션 (top_p, temperature) 조정으로 회피 가능.

**다음 진단 단계**: 3a (T5 generate parameter 강제) 가 가장 빠른 진단. `mGPT/` 코드 분석 + min_new_tokens injection 시도.

## 해결 방향 우선순위 정정 (2026-05-19)

1. **mgpt env 에 CUDA torch 설치** — 사용자 보유 RTX 5090 활용. 공식 demo.py 직접 실행으로 1단계 완전 진단 가능. inference 시간 도 분 → 초 단위 단축.
2. **공식 demo.py 의 단순 fork → 본 wrapper 와 batch 비교** — CUDA torch 설치 후 길이 0 vs 60 의 결과 비교로 length issue 정확한 위치 식별.
3. **이전 ledger 기록의 방안 C (T2M-GPT 등 alternative)** — 위 1, 2 가 실패 시.

본 진단 시도 자체는 G2 inference 가 **단순 wrapper bug 가 아니라 환경 종속 + length parameter handling 의 복합 issue** 임을 노출. mgpt env 의 GPU 활성화 없이는 진단 자체가 시간상 비효율.

## 4차 진단 — Clean install plan (사용자 directive 2026-05-19)

사용자 directive 10단계 plan ([reports/2026-05-19f.md](../../reports/2026-05-19f.md) 인용):

| Step | 내용 | 본 ledger 상의 진행 상태 |
|---|---|---|
| 1 | 기존 `external_assets/MotionGPT/` 보존 (진단 evidence + rollback 안전) | ✅ 완료 (변경 없이 그대로 유지) |
| 2 | `external_assets/MotionGPT_clean/` 새로 clone | ✅ 완료 (`git clone --depth=1 https://github.com/OpenMotionLab/MotionGPT.git`, 9.1MB code only) |
| 3 | `mgpt-clean` env 생성 (python 3.10) | 진행 예정 |
| 4 | CUDA torch 설치 (RTX 5090 활용) | 진행 예정 — RTX 5090 의 compute capability 9.0+ 호환 CUDA wheel 선택 (cu126/cu128) 필요 |
| 5 | 공식 deps 재설치 (`flan-t5-base`, `checkpoint`, `smpl`, `glove`, `t2m mean/std`) | 진행 예정 — AGENTS.md §2-1 의무 절차 그대로 |
| 6 | 원본 HumanML3D 를 `MotionGPT_clean/datasets/humanml3d` 로 junction 연결 | 진행 예정 |
| 7 | 공식 demo.py 를 먼저 실행 (clean baseline) | 진행 예정 — t2m_mini.txt 또는 t2m.txt 로 |
| 8 | 정상 길이 output 확인 (60+ frames expected) | 측정 대기 |
| 9 | wrapper 경로 옵션 `--motiongpt-root external_assets/MotionGPT_clean` 으로 지정 | 진행 예정 (wrapper 의 `MotionGPT_G2(motiongpt_root=...)` 인자는 이미 지원) |
| 10 | 본 W-2026-001 ledger 에 결과 박제 (`resolved by clean install` 또는 `still open`) | 8 단계 결과 시점에 박제 |

### Clean install plan 의 진단 가설 분리

| 4-frame 원인 후보 | clean install 결과 예측 |
|---|---|
| **기존 MotionGPT/ 에셋의 partial download 또는 corruption** | clean install 후 **정상 60+ frames** → `resolved by clean install` |
| **공식 MotionGPT 자체의 본질적 한계** (요약본 known issue) | clean install 후도 **여전히 4 frames** → `still open`, 진단 방향을 mGPT/models/ 코드 수정 (방안 A) 으로 |
| **CUDA torch 부재 + CPU inference 의 numerical issue** | CUDA torch 설치 후 (Step 4) 정상 → `resolved by GPU` |

본 plan 은 위 3 후보를 **각 단계의 결과로 점진 분리**. 단계별 결과를 본 ledger 의 "변경 이력" 에 append-only 박제.

## 변경 이력 (append-only)

- 2026-05-19 — 등록. Action 2 smoke test 에서 발견. severity `critical`, status `open`.
- 2026-05-19 (1차 진단 후) — 진단 1단계 (CUDA 미설치 부분 막힘) + 2단계 (length=0 patch 3시간 hang 검증 미완) 결과 박제. patch rollback. 해결 방향 우선순위 정정. status `open` 유지.
- 2026-05-19 (2차 진단 후) — 사용자 directive "다시 실행" 으로 length=0 재시도 (wrapper subprocess 우회, 절대경로). 30초 정상 완료, **length=0 도 4-frame degenerate output** 확인 → length issue 의 진짜 원인은 wrapper 의 batch['length'] 값이 아니라 MotionGPT 내부 T5 LM generate. 진단 3단계 진입 방향 (3a T5 generate parameter / 3b vocab 확장 / 3c sampling stopping) 명시. patch rollback (length=0 / length=60 결과 동일이라 분기 무의미). status `open` 유지.
- 2026-05-19 (3차 진단 — seed/prompt sweep) — 사용자 의심 "이전 연구 에셋이 4 frame 원인" 검증 위해 seed 4종 (1, 7, 100, 1234) × prompt 2종 ("a man is dancing", "a person walks forward then runs and jumps high") 으로 8 trials. **모든 trial 결과 (4, 22, 3) = 4 frames** — deterministic abnormal behavior, seed/prompt 종속 아님. README line 351 의 "1 motion token = 4 frames" 와 결합 시 LM 이 **딱 1 motion token 만 생성하고 stop**. 사용자 의심 검증 결과: MotionGPT 모델 에셋 (checkpoint, T5, smpl, t2m mean/std) 은 fresh install 이라 이전 연구 에셋 corruption 가설 직접 증거 약함. 진짜 원인 후보 재정렬 → **3b (vocab 확장된 motion token embedding/lm_head 가 state_dict 로 덮어지지 않음)** 가 가장 유력 (transformers 경고 "new embeddings/lm_head will be initialized from multivariate normal" 일치). status `open` 유지.
- 2026-05-19 (4차 진단 — clean install 10단계 완료) — Step 3-8 진행 완료. **결과: 사용자 가설 기각, status `still open`**.
  - Step 3 ✅ mgpt-clean env (python 3.10)
  - Step 4 ✅ torch 2.11.0+cu128 (RTX 5090 sm_120, cuda=True 검증)
  - Step 5 ✅ requirements.txt + 보조 (bert_score, gdown, spacy en_core_web_sm) — transformers 5.8.1, pytorch_lightning 2.6.1, chumpy 0.70 정상 설치
  - Step 5b ✅ flan-t5-base 7.8GB (HuggingFace LFS), smpl 503MB, glove 20MB, t2m 2.2GB, MotionGPT-base checkpoint 1.24GB 재설치
  - Step 6 ✅ HumanML3D junction
  - Step 7 ✅ 공식 `demo.py` 실행 성공 (GPU 6초, no error)
  - Step 8 ❌ **결과 동일 4 frames**:
    ```
    sample 0 OUT: shape=(1, 4, 22, 3), abs_sum=93.7402
    sample 1 OUT: shape=(1, 4, 22, 3), abs_sum=93.7402
    ```
    두 prompt 가 byte-identical 결과 — LM 이 prompt 무시하고 같은 token sequence 생성.
  - **Checkpoint sha256 비교**: 기존 `MotionGPT/` 의 ckpt = clean `MotionGPT_clean/` 의 ckpt = `0d8a9e1a0c3bd15ed2d2160a22abe3edef299920eb9565a7e9decf1da4c8e79c` (byte-identical) → checkpoint corruption 가설 **확실히 기각**.
  - **진짜 원인 재추정**: transformers 5.8.1 (최신) 의 `resize_token_embeddings` 시 새 token (motion vocab) 의 embedding/lm_head 가 `"new embeddings will be initialized from multivariate normal"` 경고 — **MotionGPT-base 학습 시점 transformers 버전 (구버전, 추정 transformers 4.x) 의 state_dict 가 5.8.1 의 vocab resize 결과와 incompatible**. checkpoint 의 motion token weight 가 정상 load 되지 못해 random initialized embedding 그대로 inference 사용 → LM 이 prompt-independent random token (대부분 EOS) 즉시 sample → 1 token (4 frame) collapse.
  - **다음 진단 방향**: transformers 다운그레이드 (예: 4.30 또는 MotionGPT requirements.txt 가 학습 시점 사용한 버전 확인) + 재시도. 또는 load_state_dict(strict=False) 로 missing key 확인 후 motion vocab embedding 직접 load.
  - Step 9-10 미수행 — Step 8 결과로 wrapper 경로 변경 의미 없음 (clean install 도 4 frame).
  - W-2026-001 status: `still open`, severity `critical` 유지. resolution_target 정정: transformers 버전 호환성 검증.
