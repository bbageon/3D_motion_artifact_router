# MDM (G1, diffusion) deps. 기존 MDM smoke 가 통과한 momask env 구성을 기준으로 한다
# (setup.md §5-2: momask env + moviepy/spacy/CLIP). torch 는 Dockerfile 에서 cu128,
# chumpy 는 setuptools<58 로 별도 설치.
numpy==1.23.5
scipy==1.10.1
matplotlib==3.7.5
pandas==2.0.3
tqdm
ftfy
regex
blobfile
joblib
einops
smplx==0.1.28
spacy==3.5.4
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.5.0/en_core_web_sm-3.5.0-py3-none-any.whl
git+https://github.com/openai/CLIP.git
