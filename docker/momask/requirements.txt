# MoMask inference 전용 의존성 (joint generation 경로만 — smplx/chumpy/spacy 등
# mesh/visualization/eval 전용 패키지는 제외). torch/torchvision 은 Dockerfile 에서
# cu128 index 로 별도 설치한다.
numpy==1.23.5
scipy==1.10.1
einops==0.6.1
vector-quantize-pytorch==1.6.30
ftfy
regex
tqdm
PyYAML==6.0
matplotlib==3.7.5
git+https://github.com/openai/CLIP.git
